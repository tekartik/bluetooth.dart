# bluetooth

Bluetooth common

# Setup

```yaml
dependencies:
  tekartik_bluetooth:
    git:
      url: git://github.com/tekartik/bluetooth.dart
      path: bluetooth
      ref: null_safety
    version: '>=0.2.1'
```